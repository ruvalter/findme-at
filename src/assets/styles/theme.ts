import { createMuiTheme } from '@material-ui/core/styles';

export const mainTheme = createMuiTheme({
  palette: {
    primary: {
      main: '#384259',
    },
    secondary: {
      main: '#fc5185',
    },
  },
});
